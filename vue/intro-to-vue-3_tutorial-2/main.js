// const app = Vue.createApp({Options Object})

const app = Vue.createApp({
    data() {
        return {
            product: 'Socks',
            description: 'A well-designed pair of sock',
        }
    }
})